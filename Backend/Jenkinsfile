pipeline {
    agent any

    stages {
        stage('Gitlab Clone') {
            steps {
                // GitLab에서 소스를 클론하는 단계
                git(
                    branch: 'master', 
                    credentialsId: 'aseongjun99', 
                    url: 'https://lab.ssafy.com/s12-webmobile1-sub1/S12P11A506.git'
                )
            }
        }
    }
}
