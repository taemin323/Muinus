spring:
  application:
    name: muinus

  datasource:
    url: jdbc:mysql://localhost:3306/hexa?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: ssafy
    password: ssafy
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # 운영 환경이라면 'none' 또는 'validate' 권장
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
#    properties:
#      hibernate:
#        dialect: org.hibernate.spatial.dialect.mysql.MySQL8SpatialDialect # Point 타입 매핑
